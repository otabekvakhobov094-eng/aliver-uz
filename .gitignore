node_modules/
dist/
.next/
.turbo/
coverage/
*.log
.env
.env.*
# Namuna fayllarda sir yo'q — ular repozitoriyda bo'lishi SHART,
# chunki o'rnatish yo'riqnomasi aynan ulardan nusxa olishni aytadi.
!.env.example
!.env.*.example
.DS_Store

# Lokal ishlab chiqishda node_modules ni boshqa joydan ulash uchun symlink
# qilinishi mumkin — u hech qachon commit qilinmaydi.

# TypeScript qurilish keshi — har bir buildda o'zgaradi, commitga kerak emas.
*.tsbuildinfo

# Git bundle fayllari repo papkasiga yoziladi (kod uzatish uchun) —
# ular hech qachon commit qilinmaydi.
*.bundle
